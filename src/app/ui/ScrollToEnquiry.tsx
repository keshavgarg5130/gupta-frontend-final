const ScrollToEnquiry = () => {
    const text = 'ENQUIRE NOW!!'
    return (
        <a href="#enquiryForm" className="group fixed -right-[43px] md:-right-[40px] top-[50%] translate-y-[-50%] bg-themeBlue px-2 py-2 rounded-t-lg text-white text-sm -rotate-90 hover:bg-white hover:text-themeBlue transition-all hover:border border-themeBlue">
            {text}
        </a>

    )
}

export default ScrollToEnquiry;
